/* ============================================================
   项目配置:API 地址 + 演示账号
   前后端同源部署时 apiBase 留空即可(走 /web/ 挂载访问);
   前后端分离时改成后端地址,如 "http://127.0.0.1:8000"
   ============================================================ */
const CONFIG = {
  apiBase: "",
  chatApi: "/api/chat/ask",
  healthApi: "/api/health",
  // 演示账号(前端模拟认证,后端暂无认证接口)
  demoUsers: [
    { username: "admin", password: "123456" },
  ],
};
